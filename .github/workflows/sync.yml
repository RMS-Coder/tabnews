name: Sincronizar clone-tabnews para tabnews

on:
  schedule:
    - cron: '*/30 * * * *'  # Executa a cada 30 minutos
  workflow_dispatch:        # Permite execução manual

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do repositório destino
        uses: actions/checkout@v3

      - name: Clonar repositório de origem
        run: |
          git clone https://github.com/RodrigoMS/clone-tabnews.git origem
          rsync -av origem/ ./ --exclude=.git

      - name: Commit e push
        run: |
          git config user.name "GitHub Action" 
          git config user.email "action@github.com" 
          git add . git commit -m "Sync automático do clone-tabnews" || echo "Nada novo para commitar" 
          git push https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/RMS-Coder/tabnews.git HEAD:main

